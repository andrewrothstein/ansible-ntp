---
ntp_upstream:
  - 0.pool.ntp.org
  - 1.pool.ntp.org
  - 2.pool.ntp.org
  - 3.pool.ntp.org

ntp_restrict:
  - '127.0.0.1'
  - '::1'

ntp_driftfile: /var/lib/ntp/drift
chrony_driftfile: /var/lib/chrony/drift

ntp_activate: true
